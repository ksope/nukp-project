const navLinks = document.getElementById("nav-links");

//show menu function
const showMenu = () => {
    navLinks.style.right = "0";
};

const hideMenu = () => {
    navLinks.style.right = "-200px";
};
